CREATE EXTENSION IF NOT EXISTS vector;

-- Example table for products with vector embeddings
-- CREATE TABLE IF NOT EXISTS products (
--     id SERIAL PRIMARY KEY,
--     name TEXT NOT NULL,
--     description TEXT,
--     price NUMERIC(10, 2),
--     embedding vector(768) -- assuming a 768-dimensional embedding
-- );
